// Shared inline style tokens for the site sections.
export const ui = {
  lead: { color: 'var(--muted)', fontSize: 'clamp(16px, 2vw, 19px)', maxWidth: '62ch', margin: '16px 0 0' },
  card: { background: 'var(--surface)', border: '1px solid var(--border)', borderRadius: 14, padding: '22px 24px' },
  cta: {
    display: 'inline-block',
    marginTop: 30,
    fontFamily: 'var(--font-mono)',
    fontSize: 14,
    color: 'var(--accent)',
    border: '1px solid var(--accent)',
    borderRadius: 999,
    padding: '10px 22px',
  },
};
